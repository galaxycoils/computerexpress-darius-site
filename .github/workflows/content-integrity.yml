name: Content integrity

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: '23 12 * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: content-integrity-${{ github.ref }}
  cancel-in-progress: false

jobs:
  integrity:
    name: ${{ matrix.suite }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - suite: collector-regression
            command: node --test scripts/*.regression.mjs
          - suite: content-audit
            command: npm run content:audit
          - suite: unit-tests
            command: npm test
          - suite: production-build
            command: npm run build

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: ${{ matrix.suite }}
        run: ${{ matrix.command }}

  integrity-passed:
    name: Content integrity passed
    runs-on: ubuntu-latest
    needs: integrity
    if: always()
    steps:
      - name: Aggregate suite results
        run: |
          if [ "${{ needs.integrity.result }}" != "success" ]; then
            echo "::error::Content integrity matrix failed"
            exit 1
          fi
          echo "All content integrity suites passed"
